export default function ErrorMessage() {
  return (
    <p>
      Oops! It seems like a glitch sneaked into the system. To fix it, simply
      press the refresh button in your browser.
    </p>
  );
}
